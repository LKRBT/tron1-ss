#机器人几何参，如果机器人是圆形，设置 robot_radius,如果是其他形状设置 footprint
robot_radius: 0.4 #圆形
#footprint: [[-0.369, -0.25], [-0.369, 0.25], [0.369, 0.25], [0.369, -0.25]] #其他形状

obstacle_range: 4.0 # 用于障碍物探测，比如: 值为 3.0，意味着检测到距离小于 3 米的障碍物时，就会引入代价地图
raytrace_range: 4.5 # 用于清除障碍物，比如：值为 3.5，意味着清除代价地图中 3.5 米以外的障碍物
 
#膨胀半径，扩展在碰撞区域以外的代价区域，使得机器人规划路径避开障碍物
# TODO : 장애물 회피에서 다시 테스트 필요
inflation_radius: 0.2 # fix
# inflation_radius: 0.1

#代价比例系数，越大则代价值越小
# TODO : 장애물 회피에서 다시 테스트 필요
# cost_scaling_factor: 1.0
cost_scaling_factor: 10.0 # fix
 
#地图类型
map_type: costmap
#导航包所需要的传感器
observation_sources: scan
# 25.08.25 [BDS]
# observation_sources: scan scan_depth
# observation_sources: scan depth

#对传感器的坐标系和数据进行配置。这个也会用于代价地图添加和清除障碍物。例如，你可以用激光雷达传感器用于在代价地图添加障碍物，再添加kinect用于导航和清除障碍物。
#sensor_frame should be your own frame in tftree
# scan: {sensor_frame: body, data_type: LaserScan, topic: /scan, marking: true, clearing: true, min_obstacle_height: -0.0, max_obstacle_height: 10000}
scan: {sensor_frame: body, data_type: LaserScan, topic: /scan, marking: true, clearing: true} # fix
livox_data:
    data_type: PointCloud2
    topic:  /livox/lidar_filter #/livox/lidar
    marking: true
    clearing: true
    # TODO : 사용시, 라이다 설치 방향에 따라서 반대로 설정하는 것 검토 필요
    min_obstacle_height: 0.3
    max_obstacle_height: 0.5
# 25.08.25 [BDS]
# TODO : Realsense 설정
# scan_depth:
#     data_type: LaserScan
#     topic: /scan_depth
#     marking: true
#     clearing: true
#     sensor_frame: camera_link
# 25.08.25 [BDS]
# depth:
#     data_type: PointCloud2
#     topic: /camera/depth/color/points
#     marking: true
#     clearing: true
#     # sensor_frame: camera_link
#     sensor_frame: camera_depth_optical_frame
#     # min_obstacle_height: -0.7      # 카메라보다 아래 0.9m → 즉 바닥 ~ 약 10cm 높이부터 감지
#     # max_obstacle_height: 0.3       # 카메라보다 위로 0.3m까지 감지
#     min_obstacle_height: 0.5
#     # max_obstacle_height: 3.0
#     max_obstacle_height: 1.5

# # origin_z: 0.1
# # z_resolution: 0.05
# # z_voxels: 14                # 총 1.0 m 커버(0.1 ~ 0.8 m)
# # unknown_threshold: 1
# # mark_threshold: 13
# # publish_voxel_map: false
# # combination_method: 1